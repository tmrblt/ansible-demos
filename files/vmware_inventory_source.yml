---
compose:
  ansible_host: guest.ipAddress
  ansible_connection: "'winrm' if 'win' in config.guestId else ssh"
  ansible_port: "'5985' if 'win' in config.guestId else 22"
  ansible_winrm_transport: "'ntlm' if 'win' in config.guestId else None"
hostnames:
- 'config.name'
filters:
  - runtime.powerState == "poweredOn"
properties:
- config
- runtime
keyed_groups:
- key: tag_category.OS
  prefix: "vmware_tag_os_category_"
  separator: ""
groups:
  linux: "'rhel' in config.guestId or 'centos' in config.guestId or 'ubuntu' in config.guestId or 'debian' in config.guestId or 'sles' in config.guestId or 'suse' in config.guestId"
  windows: "'win' in config.guestId"
with_nested_properties: false
