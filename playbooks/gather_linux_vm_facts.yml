---
- name: gather linux vm facts
  hosts: linux
  gather_facts: true
  tasks:
  - name: set vm facts
    set_fact:
      vm_list: "{{for host in groups['windows'] hostvars[inventory_hostname].ansible_hostname endfor }}"
  - name: print vm_list
    debug:
      var: vm_list
