---
- name: gather linux vm facts
  hosts: linux
  gather_facts: true
  tasks:
  - name: gather facts
    debug:
      var: ansible_facts
  - name: set vm facts
    set_fact:
      vm_list: "{{hostvars[host].ansible_hostname}}"
